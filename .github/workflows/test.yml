name: tests

# Opt JS-based actions (checkout, setup-python) onto Node.js 24.
# Node 20 is deprecated on GitHub-hosted runners and will be removed 2026-09-16.
# Remove this once actions/* publish versions that ship Node 24 natively.
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

on:
  push:
    branches: [master, main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run pytest
        run: pytest tests/ -v
        env:
          # Tests mock the providers, so these aren't really used —
          # but the config dataclass loads on import, so non-empty values
          # avoid any surprise key validation in future changes.
          OPENAI_API_KEY: test-key
          ANTHROPIC_API_KEY: test-key
          GOOGLE_API_KEY: test-key
